pip install script:
pip install -r requirements.txt --user

*Note: first go to the folder location in commant prompt (or any other shell) in which you have the file requirements.txt before executig the script. 